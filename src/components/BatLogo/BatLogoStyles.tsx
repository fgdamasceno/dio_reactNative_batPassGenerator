import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 30,
    color: "#e5bf3c",
  },
  image: {
    resizeMode: "contain",
    height: 180,
  },
});

export default styles;
